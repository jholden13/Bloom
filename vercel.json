{
  "buildCommand": "pnpx convex deploy --cmd 'pnpm run build:deploy'",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
